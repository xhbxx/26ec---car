# 基于 MSPM0G3507 的智能小车控制系统算法说明

## 1. 项目信息

| 项目 | 内容 |
|---|---|
| 题目名称 | 2026 年电赛训练题智能小车控制 |
| 主控 | 天猛星 MSPM0G3507 |
| 电机驱动 | TB6612 双路直流电机驱动 |
| 姿态传感器 | ATK-MS6DSV，独立软件 I2C |
| 编码器 | 13 线，减速比 28:1，单路 A 相上升沿计数 |
| 车轮直径 | 65 mm |
| 实现模式 | Mode 2、Mode 4、Mode 5 |

## 2. 系统总体方案

系统采用“任务状态机 + 左右轮速度闭环 + 陀螺仪航向修正”的分层控制结构。

1. 主程序负责陀螺仪初始化、按键选模、OLED 显示及周期调度。
2. 底盘状态机按照 AB 直线、BC 半圆、CD 直线、DA 半圆和停车阶段切换。
3. 编码器计算左右轮实际转速和累计里程，增量式 PI 调节 PWM。
4. 陀螺仪积分得到累计航向角，在直线和圆弧阶段形成左右轮差速修正。
5. 四路红外不参与连续循迹，仅在 Mode 5 接近终点时确认黑色横线。

程序状态流程如下：

```text
上电 -> 陀螺仪校准(LOADING) -> 模式选择

Mode 2: AB直线 -> BC半圆 -> CD直线 -> DA半圆 -> 预测急停
Mode 4: AB直线 -> 平滑停车
Mode 5: AB直线 -> BC半圆 -> CD直线 -> DA半圆
        -> 终点横线确认/里程兜底 -> 平滑停车
```

## 3. 编码器速度闭环

### 3.1 里程计算

编码器安装在电机轴上，只采集 A 相上升沿。减速箱输出轴每转一圈的计数为：

```text
N = 13 x 28 = 364 脉冲/圈
```

单轮累计距离：

```text
S = count x pi x D / N
```

其中 `D=65 mm`。车体中心里程取左右轮累计距离的平均值，降低单轮打滑和计数误差的影响。

### 3.2 速度 PI

系统每 `50 ms` 读取一次左右编码器脉冲并换算实际转速。左右轮分别执行相同的增量式 PID：

```text
e(k) = target_rpm(k) - actual_rpm(k)

Delta_u(k) = Kp[e(k)-e(k-1)]
             + Ki e(k)
             + Kd[e(k)-2e(k-1)+e(k-2)]

u(k) = u(k-1) + Delta_u(k)
```

当前参数：

| 参数 | 数值 | 作用 |
|---|---:|---|
| Kp | 0.40 | 提高速度跟随能力 |
| Ki | 0.20 | 消除左右轮稳态速度误差 |
| Kd | 0 | 编码器脉冲较稀疏，关闭微分以减少噪声 |
| PID 周期 | 50 ms | 编码器速度采样周期 |
| PWM 追加余量 | 15% | 限制 PI 输出，防止突然满占空比 |

## 4. 陀螺仪航向算法

### 4.1 上电校准与角度积分

上电后 OLED 只显示 `LOADING`，车辆保持静止并采集 `40` 个零偏样本。校准完成后才允许选择模式和启动。

陀螺仪 Z 轴原始值先减去静止零偏，再进行死区处理：

```text
gyro_corrected = (gyro_z - gyro_bias) x direction_sign
omega = gyro_corrected x 70 mdps
theta(k) = theta(k-1) + omega x Delta_t
```

陀螺仪更新周期为 `10 ms`，每次任务启动时将累计角度清零。

### 4.2 直线航向修正

AB 段目标航向为 `0°`，CD 段目标航向为 `180°`，最终接近段目标航向为 `360°`。

```text
heading_error = target_angle - measured_angle
correction = limit(K_heading x heading_error)

left_speed  = base_speed + correction
right_speed = base_speed - correction
```

Mode 2 出弯后不经过转向缓变，立即建立回正差速。当前 Mode 2 直线航向比例为 `10 mm/s/°`，最大半差速修正为 `55 mm/s`。

### 4.3 圆弧控制

圆弧使用“编码器基础差速 + 陀螺仪角度修正”。根据圆弧已行驶距离生成理论目标角度：

```text
curve_target = curve_start_angle
             + curve_distance / 1571 x 180°

gyro_correction = limit[K_curve x
                         (curve_target - measured_angle)]

left_speed  = curve_speed + base_steering + gyro_correction
right_speed = curve_speed - base_steering - gyro_correction
```

基础差速保证车辆持续转弯，陀螺仪只修正实际转角相对理论转角的误差。Mode 2 第一弯累计角度到达 `180°` 时立即清除弯道差速并进入 CD 直线；如果陀螺仪异常，则以编码器半圆距离 `1571 mm` 作为保险条件。

## 5. 各模式实现逻辑

### 5.1 Mode 2：整圈快速运行并定点急停

Mode 2 不进行钢球控制，目标是在规定时间内完成整圈并停在指定位置。

1. 速度目标直接下发，不使用平滑加速斜坡。
2. AB、CD 直线分别由编码器里程切换，CD 距离单独使用 `TASK2_CD_STRAIGHT_MM`。
3. 两个半圆采用较大的基础轮速差，并叠加较强的陀螺仪角度修正。
4. 第一弯到达 `180°` 立即停止转向，强制进入 `180°` 直线航向闭环。
5. 第二弯接近终点时，根据当前实测速度预测制动距离：

```text
brake_distance = v^2 / (2a_brake) + brake_margin
```

当“当前总里程 + 预测制动距离”达到 Mode 2 整圈目标里程时，两轮立即设为 0，并使用 TB6612 短刹车。

### 5.2 Mode 4：直线平稳运行

1. 只执行 AB 直线，不进入圆弧状态。
2. 目标速度使用线性斜坡逐步建立，减小启动冲击。
3. 编码器速度 PI 保证左右轮实际速度接近目标速度。
4. 陀螺仪维持 `0°` 航向，不使用红外连续纠偏。
5. 达到 AB 目标距离后按设定减速度平滑停车。

### 5.3 Mode 5：整圈平稳运行

1. 路径状态与 Mode 2 相同，但速度、加速度、转弯差速和陀螺仪修正均更温和。
2. 中心速度和转向差速均采用斜坡限制，降低钢球受到的冲击。
3. CD 直线距离由 `TASK5_CD_STRAIGHT_MM` 独立设置，不受 Mode 2 参数影响。
4. 接近终点时连续检测三次黑色横线才确认终点，避免单次红外干扰。
5. 未检测到横线时使用编码器整圈里程加兜底余量停车。

## 6. 当前关键参数

| 参数 | Mode 2 | Mode 4 | Mode 5 |
|---|---:|---:|---:|
| 直线速度 | 480 mm/s | 220 mm/s | 270 mm/s |
| 弯道速度 | 450 mm/s | 不使用 | 230 mm/s |
| 基础弯道半差速 | 135 mm/s | 不使用 | 40 mm/s |
| 直线陀螺仪 Kp | 10 mm/s/° | 2 mm/s/° | 2 mm/s/° |
| 直线陀螺仪限幅 | 55 mm/s | 25 mm/s | 25 mm/s |
| 弯道陀螺仪 Kp | 10 mm/s/° | 不使用 | 2 mm/s/° |
| 弯道陀螺仪限幅 | 50 mm/s | 不使用 | 20 mm/s |
| 加速度 | 直接设速 | 95 mm/s² | 55 mm/s² |
| 减速度 | 立即短刹 | 80 mm/s² | 70 mm/s² |
| 时间保护 | 20 s | 8 s | 30 s |

场地参数：

| 参数 | 数值 |
|---|---:|
| AB 直线 | 1510 mm |
| Mode 2 CD 直线 | 1460 mm |
| Mode 5 CD 直线 | 1440 mm |
| 半圆中心线长度 | 1571 mm |
| 半圆目标转角 | 180° |

注意：根据程序中的 `125 rpm` 最大输出轴转速和 `65 mm` 轮径，软件换算的最大有效轮速约为 `425 mm/s`。Mode 2 中高于此值的速度设定会在电机驱动层被限幅，提交前应以 OLED 的 `SET/ACT` 和实测时间为准。

## 7. 软件可靠性设计

1. 陀螺仪未完成校准时禁止按键启动。
2. 陀螺仪通信异常时，圆弧状态仍可由编码器距离退出。
3. 每个模式均设置最长运行时间，超时后进入停车状态。
4. Mode 5 终点红外采用连续多次确认，并保留编码器里程兜底。
5. Mode 2 根据实际速度动态预测制动点，减少固定提前量受电池电压影响的问题。
6. OLED 显示左右轮设定速度、实际速度、任务状态、IMU 状态和累计角度，便于现场调试。

## 8. 测试记录格式

提交前至少进行三次重复测试，并填写下表：

| 模式 | 次数 | 完成时间 | 停车位置误差 | 最大航向误差 | 钢球状态 | 结果 |
|---|---:|---:|---:|---:|---|---|
| Mode 2 | 1 |  |  |  | 不考核 |  |
| Mode 2 | 2 |  |  |  | 不考核 |  |
| Mode 2 | 3 |  |  |  | 不考核 |  |
| Mode 4 | 1 |  |  |  |  |  |
| Mode 4 | 2 |  |  |  |  |  |
| Mode 4 | 3 |  |  |  |  |  |
| Mode 5 | 1 |  |  |  |  |  |
| Mode 5 | 2 |  |  |  |  |  |
| Mode 5 | 3 |  |  |  |  |  |

## 9. 提交材料清单

1. 本算法说明文档。
2. 完整 CCS 工程源代码和 `empty.syscfg` 引脚配置。
3. 可烧录的 `.out` 或 `.txt` 固件。
4. 三个模式的现场测试数据和运行视频。
5. 硬件接线图，标明电机、编码器、OLED、按键、红外及陀螺仪引脚。
