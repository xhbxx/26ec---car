# 小车运动数据串口遥测协议 V1

## 1. 接线与串口参数

```text
小车 PA10（UART0_TX） -> 小球控制单片机 RX
小车 GND              -> 小球控制单片机 GND

波特率：115200
数据格式：8 数据位，1 停止位，无校验（8N1）
电平：3.3 V TTL
发送周期：10 ms（100 Hz）
字节序：小端序，低字节先发送
```

PA11 已配置为 UART0_RX，但当前协议只发送数据，PA11 可以不接。

## 2. 固定 48 字节数据帧

| 偏移 | 长度 | 字段 | 数据类型 | 单位/说明 |
|---:|---:|---|---|---|
| 0 | 2 | 帧头 | uint8[2] | 固定 `AA 55` |
| 2 | 1 | 协议版本 | uint8 | 固定 `01` |
| 3 | 1 | 总帧长度 | uint8 | 固定 `30`（十进制 48） |
| 4 | 2 | 帧序号 | uint16 | 每发送一帧加 1，自然回绕 |
| 6 | 4 | 时间戳 | uint32 | ms |
| 10 | 1 | 任务模式 | uint8 | 2、4 或 5 |
| 11 | 1 | 底盘状态 | uint8 | 见第 3 节 |
| 12 | 1 | 有效状态标志 | uint8 | 见第 4 节 |
| 13 | 1 | 保留 | uint8 | 固定 0 |
| 14 | 2 | 目标车体速度 | int16 | mm/s，前进为正 |
| 16 | 2 | 实际车体速度 | int16 | mm/s，左右编码器平均 |
| 18 | 2 | 左轮实际速度 | int16 | mm/s，车辆前进为正 |
| 20 | 2 | 右轮实际速度 | int16 | mm/s，车辆前进为正 |
| 22 | 2 | 目标纵向加速度 | int16 | mm/s²，目标速度差分 |
| 24 | 2 | 编码器实际加速度 | int16 | mm/s²，实测速度差分 |
| 26 | 2 | IMU X 轴加速度 | int16 | mg，传感器坐标系 |
| 28 | 2 | IMU Y 轴加速度 | int16 | mg，传感器坐标系 |
| 30 | 2 | IMU Z 轴加速度 | int16 | mg，传感器坐标系 |
| 32 | 4 | Z 轴角速度 | int32 | mdps，1000 mdps=1°/s |
| 36 | 4 | 累计航向角 | int32 | mdeg，1000 mdeg=1° |
| 40 | 4 | 累计车体里程 | int32 | mm |
| 44 | 2 | 目标转向半差速 | int16 | mm/s，`(左目标-右目标)/2` |
| 46 | 2 | CRC16-Modbus | uint16 | 低字节在前 |

所有 `int16` 和 `int32` 均为二进制补码。目标或实测加速度超过 int16 范围时，发送值会饱和到 `-32768~32767`。

## 3. 底盘状态值

```text
0 = CHASSIS_WAIT             等待
1 = CHASSIS_STRAIGHT_AB      AB 直线
2 = CHASSIS_CURVE_BC         BC 弯道
3 = CHASSIS_STRAIGHT_CD      CD 直线
4 = CHASSIS_CURVE_DA         DA 弯道
5 = CHASSIS_FINAL_APPROACH   终点接近
6 = CHASSIS_STOPPING         正在停车
7 = CHASSIS_COMPLETE         任务完成
8 = CHASSIS_ERROR            任务错误
```

## 4. 状态标志 byte[12]

```text
bit0 = 1：陀螺仪零偏校准完成
bit1 = 1：陀螺仪在线且最近通信有效
bit2 = 1：车辆正在运行
bit3 = 1：当前为直线路段
bit4 = 1：当前为弯道路段
bit5 = 1：正在停车
bit6 = 1：编码器已经产生有效速度采样
bit7：保留
```

小球控制单片机只有在 `bit0=1`、`bit1=1` 且 CRC 正确时才能使用 IMU 数据。`bit6=0` 时不能使用编码器加速度。

## 5. 数据计算定义

```text
实际车体速度 = (左轮实际速度 + 右轮实际速度) / 2

编码器实际加速度 =
    (当前车体实测速度 - 上次车体实测速度) / 编码器采样时间

目标纵向加速度 =
    (当前车体目标速度 - 上次车体目标速度) / 10 ms

目标转向半差速 =
    (左轮目标速度 - 右轮目标速度) / 2
```

编码器速度每 50 ms 更新，因此编码器加速度适合低频趋势和零漂校正；IMU 加速度每 10 ms 更新，适合小球控制的快速前馈。IMU 三轴数据包含重力和安装倾角，接收端必须在车辆静止时记录零点，并根据模块安装方向选出纵向轴。

## 6. CRC16-Modbus

CRC 只计算 `byte[2]~byte[45]`，共 44 字节，不包含帧头和 CRC 本身。

```c
uint16_t telemetry_crc16(const uint8_t *data, uint8_t length)
{
    uint16_t crc = 0xFFFFU;

    while (length-- != 0U) {
        uint8_t bit;
        crc ^= *data++;
        for (bit = 0U; bit < 8U; bit++) {
            crc = (crc & 1U) != 0U
                ? (uint16_t)((crc >> 1U) ^ 0xA001U)
                : (uint16_t)(crc >> 1U);
        }
    }
    return crc;
}
```

校验结果发送顺序：

```text
byte[46] = crc & 0xFF
byte[47] = crc >> 8
```

## 7. 接收端处理要求

1. 在字节流中搜索连续的 `AA 55`。
2. 检查 `byte[2]==1`、`byte[3]==48`。
3. 收满 48 字节后计算 CRC16。
4. CRC 正确才更新小球控制输入，错误则丢弃整帧并重新搜索帧头。
5. 通过帧序号判断丢帧，uint16 从 65535 回到 0 属正常回绕。
6. 连续 50 ms 未收到有效帧时，接收端进入通信失效保护。
7. 小球前馈优先使用目标加速度和 IMU 纵向加速度，编码器加速度用于低频修正。

